// const bibleAxios = axios.create({
//   baseURL: 'http://localhost:4000',
//   timeout: 30000,
//   headers: {'authorization': 'Bearer token'}
// });

bibleAxios.get('/user/profile').then;

bibleApiaxios.get('/askfhakjh');

const bibleAxios = () => {
    if (StorageEvent.goken) {
        token = '234';
    }
    axios.create({
        baseURL: 'http://localhost:4000',
        timeout: 30000,
        headers: { authorization: `Bearer ${token}` },
    });
};

bibleAxios().get('/user/profile').then;
